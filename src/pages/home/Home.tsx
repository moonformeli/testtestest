import * as React from "react";
import { observer } from "mobx-react-lite";

const Home: React.FC = () => {
  return (
    <div>
      <h1>Home</h1>
    </div>
  );
};

export default observer(Home);
